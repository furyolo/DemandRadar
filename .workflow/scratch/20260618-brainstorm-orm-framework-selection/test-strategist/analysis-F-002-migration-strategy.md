# F-002 Migration Strategy

Migration tests MUST prove behavior parity.

Required cases:
- fresh DB schema creation;
- save full pipeline result;
- list top scores;
- show demand detail;
- read run summary;
- reject invalid foreign keys.
